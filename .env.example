DATABASE_URL="postgresql://attendify:attendify@localhost:5433/attendify"
SESSION_SECRET="replace-with-a-long-random-string-at-least-32-chars"
# Optional. Without this, OTP is printed in the server log (and shown on /verify in development).
RESEND_API_KEY=""
EMAIL_FROM="Attendify <beth.t@example.com>"
APP_URL="http://localhost:3000"
